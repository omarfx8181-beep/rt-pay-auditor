# RT Pay Auditor — Claude Code project memory

## What this is
A paycheck auditor PWA for a Registered Respiratory Therapist at M Health Fairview (biweekly pay, Kronos timekeeping). Enter shifts → engine computes expected gross and net → audit against the real stub → flag shortfalls in dollars AND bonus units (code 548). Born from a real dispute where a 16-hour bonus was paid at 10 of 15 units ($250 short) and nobody caught it for weeks.

v1 lives at `reference/v1-artifact.jsx` — a working single-file React artifact whose engine reconciles a real pay stub to within $0.02. Treat it as the spec made executable.

## Hard rules
1. NEVER change engine math without the golden tests passing (SPEC.md → Acceptance tests). If a change breaks them, stop and ask Omar — do not "fix" the test.
2. All money math in integer cents internally; format to dollars only at render. (v1 uses float + round-to-2; porting to cents is part of Milestone 1.)
3. Local-first, same philosophy as Knockdown: no backend, no accounts, no telemetry, nothing leaves the device. Persistence = localStorage/IndexedDB plus JSON export/import for backups to iCloud Drive.
4. Design system = Knockdown's. Milestone 0 extracts real tokens from the Knockdown repo before any UI work — do not invent a palette.
5. The schedule-scan feature calls the Anthropic API with Omar's own key, entered in Settings and stored locally only. Never hardcode, never commit; .gitignore any env files.
6. Pay rules change (bonus tiers move week to week). Everything that can change is config, not constants.

## Current state (V3, July 2026)
V3 shipped: a presentation/IA rebuild per `design/V3_DESIGN_BRIEF.md`, with one deviation Omar chose at the M2 review gate — **colors stayed Knockdown** (terracotta/cream/espresso, `design/tokens.md` §2) on the V3 structure (3-tab IA, SF Pro/Inter single family, V3 type scale, 20px cards). Do not swap the palette again without asking.

- IA: **Home "This Check"** (period picker, status hero, Check my paycheck → verdict, breakdown, what-if) · **Shifts** (cards + bottom-sheet editor, schedule scan, timecard true-up `src/lib/timecard.ts` — punches replace hours, evening credit fills `cfgDraft.eveningHours` — overtime meter) · **Goals** (`src/screens/Goals.tsx` + `src/lib/goals.ts`: per-year target gross|take-home in `settings.goals`, Knockdown fill meter, pace vs `periods.yearGridEnds` grid, plan priced via engine what-if levers, tappable year-in-bars deep-linking to stub details, and the pickup strategizer — `lengths`/`maxPickups` persisted on the `YearGoal`, each length engine-priced incl. its live tier units and double-time via `pickupValues`/`tierUnitsForLength`, `buildPickupPlan` returns the fewest-pickups/least-overshoot combo per check or, when availability can't cover, the honest ceiling + where the year lands) · **Me** (pay rules in plain rows, Advanced disclosure for all tax config, open questions, periods/data/backup, the PTO bank auditor + W-2 box check (`src/lib/pto.ts`, `src/lib/w2.ts`, cards in `src/screens/BankAndW2.tsx`; settings `pto`/`w2`), and the in-app manual `src/screens/HowTo.tsx` — "How to use this app" — which launches the guided spotlight tour `src/screens/Tour.tsx`: coach marks over the real screens, auto-offered once via `settings.tourDone`, cross-tab, skippable). First run shows a 4-screen onboarding (skippable; `settings.onboarding`).
- Every period card in Me shows its money trio (made / taken out / take-home via `periodMoney`) and a "Stub details" button that deep-links into that period's check screen (App-level `homeIntent`, targeted by period id). A period with NO shifts/leave opens in record mode (`Audit recordOnly`): neutral banner, "✓ saved" instead of deltas, no HR email — filling history never fakes a verdict.
- The workspace remounts on period switch (`key=current.id`). Anything that must SURVIVE that remount — the active tab, the period-delete undo toast — lives in the outer `App`, never inside the workspace (`e2e/workspace.spec.ts` pins this).
- The verdict is a pure lib (`src/lib/verdict.ts`, tested): green/red/amber; forgiveness is ONE-DIRECTIONAL (`lineCloseEnough` + the `closeEnough` setting, default $1): drift and overs forgive up to the setting, but paid-SHORT keeps SPEC §3.7's hard nickel and never clears green. Only EARNINGS shortfalls make a red and sum into "you're owed" — gross/net echoes and stub tax drift never double-count. Red opens a prewritten `mailto:` (generator: `src/lib/hrEmail.ts`). Off-cycle CORRECTION checks (`CorrectionDraft[]` on the period, logged in the check screen) count in `periodMoney`/rollups/anchor cross-check, and once their gross covers the shortfall the verdict becomes `corrected` ("Made whole") — partial corrections stay red with the remainder.
- Plain language is a hard rule in default views; payroll codes (548/308/320) appear only in breakdown *receipts* (two taps down), `techLabel`s, and the HR email. Label map: `src/lib/labels.ts`.
- Pay rules are preset DATA: `src/lib/presets.ts` (Facility Profile + Role, `presetSource`/`version`; Fairview RT is preset #1; `PRESETS` list feeds onboarding, which becomes a picker the moment a second preset exists). `DEFAULT_CFG_DRAFT`/`DEFAULT_TIERS` are aliases into it; a presets test pins `draftToConfig(preset.cfgDraft) === DEFAULT_CFG`.
- Scans (all with Omar's own key, browser → API only): schedule screenshots/.ics (`scan.ts`, `ical.ts`), bulk past-stub scans (`stubScan.ts` — per-stub line detail imports itemized via `scannedStubActual` when readable, bare gross/net otherwise), and current-stub line extraction that auto-fills the check (`stubFill.ts` — the mapper is code, tested; the model only reads). Scans self-route (`scanRouting.ts`, tested): stub ends that read a day or two off snap to the biweekly grid (`snapEndToGrid`, ±2d — used by `matchStubPeriod` AND `planStubImports`, so misreads never spawn overlapping periods), a stub files into the period it belongs to (created if unseen, then opened), schedules split across the current + next two periods, and each stub's YTD column — or a scanned Kronos "Year to Date" summary screen (`stubScan.parseYtdSummary` + `scanRouting.summaryToAnchor`, net derived in cents, as-of date snapped to the last PAID period on the biweekly grid) — is kept as a per-year anchor (`ytdAnchors` setting) that Me's Year card cross-checks against `ytdThroughDate` — stub-true values outrank estimates.
- Me's Year card also tells the full money story ("Where the money went"): gross → taxes / pretax / after-tax / imputed-life buckets → take-home. A summary-scanned anchor carries payroll's OWN buckets (`YtdAnchor.taxesCents` etc.) and outranks the app's per-period sums (`rollupYtd` bucket fields — stub lines beat engine estimates per line; bare gross/net periods count in `bucketSkippedCount`, shown as a "not itemized yet" row so the arithmetic stays exact, never guessed). The Year card also has a year picker (chips once a second year of data exists) and a tax-time CSV export (`src/lib/csv.ts`; `periods.periodMoney` is the shared per-period money resolver).
- The July 2026 batch of seven (each a tested pure lib + thin UI): **scoreboard** (`caught.ts` — lifetime caught/recovered/open + clean streak derived by recomputing every FINISHED period's verdict offline, never a stored counter; card on Home); **shift price tags** (`worth.ts` — marginal value = period with-vs-without the shift, OT interplay exact; ≈ tag on shift cards) and one-tap 8h/12h/16h pickup chips on the what-if card (wired to live tier units via `tierUnitsForLength`); **bonus-posting scan** (`tierScan.ts` + `TierScanPanel` in Me → Bonus tiers — model reads, code diffs with loose label keys that keep over/under distinctions, amber warning when a tier moved DOWN, apply replaces the period's tiers); **"Why is this check different?"** (`checkDiff.ts` — engine lines diffed key-by-key with each period's own rules, headline from `periodMoney`; Disclosure on Home); **live ticker** (`shiftClock.ts` — window from the schedule note's "sched HH:MM–HH:MM" (nights roll past midnight) or one-tap "I'm on now" persisted in settings key `onNow`; counting card atop Home); **Year Wrapped** (`wrapped.ts` + `screens/Wrapped.tsx` — full-screen tap-through reel from the Year card's numbers, entry on the Year card); **QR rules share** (`presetShare.ts` + `screens/ShareRules.tsx` — COMPACT positional payload (jsQR decodes only ~≤900 bytes; `CFG_KEYS` order is the schema, append-only), qrcode to render + jsQR to import, rules/tiers only, never data or keys). De-wording rule from the same batch: numbers lead, each fact stated once, policy prose lives behind Disclosures or in HowTo.
- Deploy: push to `main` → `.github/workflows/pages.yml` (tests must pass) → gh-pages → https://omarfx8181-beep.github.io/rt-pay-auditor/.

## Stack
Vite + React 19 + TypeScript + Tailwind + Framer Motion (Omar's standard web stack). Vitest for engine tests. PWA: manifest + service worker, installable on iPhone/iPad.

## Commands
`npm run dev` · `npm run test` · `npm run build`

## Working style
Omar wants meticulous, step-by-step work: show the failing/passing test output, explain what changed and why, one milestone at a time. Engine before UI, always.
