// Deliberately unstyled: M0 extracts the Knockdown design tokens before any
// UI work (CLAUDE.md rule 4). Screens land in M2.
export default function App() {
  return (
    <main>
      <h1>RT Pay Auditor</h1>
      <p>
        Milestone 1 — engine only. Run <code>npm run test</code> for the golden
        acceptance tests (SPEC.md §3). UI arrives in M2, in Knockdown tokens.
      </p>
    </main>
  );
}
